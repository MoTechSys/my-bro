# 🤝 بروتوكول التعاون بين وكلاء AI متعددين (Multi-Agent Collaboration Protocol)

> يعمل على هذا المستودع **أكثر من نموذج AI** في جلسات منفصلة (حالياً: Claude + GPT-6 Astra).
> هذا البروتوكول **إلزامي** لكل وكيل. خرقه = تعارضات Git وضياع عمل.

---

## 1. المبدأ الحاكم
**`main` هي الحقيقة الوحيدة. لا أحد يكتب على `main` مباشرة. كل وكيل يعمل على فرعه الخاص ويدمج عبر PR.**

| الوكيل | الفرع الخاص | معرّفه في السجلات |
|--------|-------------|-------------------|
| Claude (Anthropic) | `agent/claude` | `[CLAUDE]` |
| GPT-6 Astra (OpenAI) | `agent/astra` | `[ASTRA]` |
| أي وكيل جديد | `agent/<name>` | `[<NAME>]` |

> الفرع القديم `genspark_ai_developer` = فرع Claude التاريخي؛ يُعامَل كـ `agent/claude`.

---

## 2. دورة الجلسة الإلزامية (لكل وكيل، كل مرة)

```
┌─ START ─────────────────────────────────────────────────────────────────┐
│ 1. git fetch origin && git checkout agent/<me> && git rebase origin/main │
│ 2. اقرأ بالترتيب: AI_AGENT_START_HERE.md → docs/05_… → docs/00_… →      │
│    docs/TASKBOARD.md → docs/SESSIONS_LOG.md (آخر 3 جلسات)              │
│ 3. اختر مهمة من TASKBOARD حالتها 🟢 FREE وضمن نطاق ملكيتك (§3)         │
│ 4. احجزها: غيّر حالتها إلى 🔒 [ME] + التاريخ → commit + push فوراً      │
│    ("claim: T-xx by [ME]") → افتح PR صغير وادمجه فوراً (حجز = دمج سريع) │
├─ WORK ──────────────────────────────────────────────────────────────────┤
│ 5. اشتغل فقط داخل ملفات المهمة. commit صغير بعد كل خطوة منطقية.        │
│ 6. قبل أي commit يلمس wazuh/ : bash scripts/validate/validate_all.sh    │
│ 7. كل 60 دقيقة عمل أو عند أي تغيير كبير: rebase على origin/main         │
├─ END ───────────────────────────────────────────────────────────────────┤
│ 8. حدّث: TASKBOARD (✅ DONE أو ◐ IN-PROGRESS مع "المتبقي بالضبط")        │
│           SESSIONS_LOG.md (سطر واحد: من، متى، ماذا، ماذا بعد)             │
│           00_PROJECT_STATE.md (إذا تغيّرت الحالة العامة)                  │
│           04_ISSUES_LOG.md (إذا اكتشفت/أغلقت خطأً)                        │
│ 9. git rebase origin/main → حل التعارضات (لصالح main) → push -f          │
│ 10. افتح/حدّث PR إلى main → ادمجه (--merge) → شارك الرابط مع المستخدم   │
└─────────────────────────────────────────────────────────────────────────┘
```

**ممنوع:** ترك جلسة بدون تحديث TASKBOARD وSESSIONS_LOG. ممنوع ترك PR مفتوحاً غير مدموج إلا بسبب موثَّق في السجل.

---

## 3. خريطة الملكية (Ownership Map) — تمنع التصادم

| المنطقة | المالك الافتراضي | الآخر يستطيع |
|---------|------------------|--------------|
| `docs/00`–`05`, `DECISIONS.md`, `AI_AGENT_START_HERE.md`, `COLLABORATION_PROTOCOL.md` | **CLAUDE** (معمارية/قرارات) | يقترح عبر ISSUE في 04، لا يعدّل مباشرة |
| `docs/thesis/ch2`, `ch3` (نظري/تصميم) | **CLAUDE** | مراجعة لغوية فقط |
| `docs/thesis/ch4`, `ch5` + تنسيق DOCX النهائي | **ASTRA** (مستندات منسَّقة) | يراجع المحتوى التقني |
| `wazuh/**` (قواعد/إعدادات) | **مشترك بالحجز** — من يحجز المهمة يملك الملف حتى ✅ | — |
| `scripts/**`, `tests/**` | **ASTRA** (تنفيذ/تدقيق أمني) | يراجع |
| `docs/lab/UC-09..12` (توسعات جديدة) | **ASTRA** | يراجع |
| `docs/lab/UC-01..08` (موجودة) | **CLAUDE** | يصحح أخطاء واقعية مع ذكرها في 04 |
| `extension/**` | **CLAUDE** | يقترح |
| `docs/TASKBOARD.md`, `docs/SESSIONS_LOG.md`, `CHANGELOG.md` | **الكل** (append-only، سطر لكل تحديث) | — |

**قاعدة ذهبية:** لا تعدّل ملفاً خارج نطاقك إلا إذا حجزت مهمته في TASKBOARD أو كان الإصلاح خطأً واقعياً (typo/رقم خاطئ) وذكرته في commit message بـ `fix(cross-owner):`.

---

## 4. معيار الجودة المشترك (نفس المعيار لكل النماذج)

1. **صفر هلوسة:** أي حقيقة معملية تُسند إلى لقطة في `docs/sources/screenshots/` أو تُعلَّم `⚠️ UNVERIFIED`.
2. **Rule IDs:** حسب `AI_AGENT_START_HERE.md §2.2` — تحقق بـ `scripts/validate/check_rule_ids.py`.
3. **الحقيقة الحاكمة:** `docs/05_PROJECT_INTENT_UNIFIED_VISION.md` — لا تُعِد Zeek/Kibana/AI كمنفَّذ.
4. **اللغة:** عربية فصحى للتوثيق الأكاديمي + المصطلح الإنجليزي بين قوسين؛ إنجليزي للكود والـ commits.
5. **Commits:** `type(scope): description` — types: `docs|feat|fix|refactor|test|chore|claim`.
6. **لا أسرار:** لا API keys؛ استخدم `<YOUR_..._KEY>`.
7. **لا ملفات ثنائية جديدة كبيرة** (>5MB) إلا في `docs/sources/` أو `docs/lab/evidence/` بموافقة المستخدم.

---

## 5. حل التعارضات
- عند `git rebase origin/main` وظهور تعارض: **الأولوية لـ main** إلا إذا كان تغييرك هو المهمة المحجوزة نفسها.
- تعارض في `TASKBOARD.md`/`SESSIONS_LOG.md`: **احتفظ بالسطرَين** (كلاهما صحيح).
- خلاف في قرار معماري بين وكيلَين: يُكتب كـ ADR مقترح في `DECISIONS.md` بحالة `PROPOSED` ويقرره المستخدم.

---

## 6. التواصل بين الوكلاء (لا يوجد قناة مباشرة)
- القناة الوحيدة = **الملفات في المستودع**: `TASKBOARD.md` (ماذا)، `SESSIONS_LOG.md` (متى/من)، `04_ISSUES_LOG.md` (مشاكل)، `DECISIONS.md` (قرارات).
- رسالة لوكيل آخر: أضف سطراً في `SESSIONS_LOG.md` يبدأ بـ `@ASTRA:` أو `@CLAUDE:`.
- المستخدم هو الجسر البشري: ينقل البرومبتات ويحسم الخلافات.
