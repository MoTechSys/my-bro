# توزيع القوة بين الوكلاء (Agent Capabilities & Task Allocation)

> **الغرض:** كل وكيل يعمل فيما هو **أقوى** فيه فقط. ما يكون أحدهما مقيّداً فيه يُنقل للآخر فوراً دون نقاش.
> **توجيه المستخدم (2026-09-09):** "أي حاجة Astra قوي فيها خلّيه يشتغلها على النظام، وأنت اشتغل على اللي أنت قوي فيه… إذا في أشياء Claude مقيّد فيها اجعل Astra يشتغلها… أهم حاجة يتنفذ المشروع بدقة وبكمالية — مش تشينكس أو حذف أو تجاوز."
> **آخر تحديث:** 2026-09-09 (v1) — [CLAUDE]. يُحدَّث عند اكتشاف قوة/قيد جديد؛ التحديث يُسجَّل في `SESSIONS_LOG.md`.
> **المصادر:** أرقام OpenAI المنشورة (analyticsvidhya 2026-09، moneycontrol 2026-09-07) + **الأداء الملاحَظ فعلياً في هذا المستودع** (وهو الأوثق).

---

## 1. النموذجان

| | GPT-6 Astra (OpenAI) | Claude Fable 5.1 (Anthropic) |
|--|--|--|
| هوية الـ commit | `[ASTRA]` | `[CLAUDE]` |
| الجيل | خليفة GPT-5.6 Sol (2026-09) | Fable 5.x (2026) |
| التكلفة/M tokens | $10 / $50 | $5 / $25 |

## 2. مصفوفة القوة — Benchmarks منشورة + ملاحَظ في هذا المستودع

| المحور | Astra | Claude | الأقوى | الدليل |
|--------|:-----:|:------:|:------:|--------|
| استخدام الحاسوب/الواجهات (OSWorld 2.0) | 72.6% | ~70% | Astra | منشور |
| كود الطرفية/الأدوات (Terminal-Bench 4.0) | 57.7% | 55.8% | ≈ | منشور |
| رياضيات/إحصاء صعب (FrontierMath T4) | 97.6% | 87.8% | **Astra** | منشور |
| استدلال واسع متعدد المجالات (HLE w/ tools) | 57.2% | **65.0%** | **Claude** | منشور |
| أمن سيبراني (ExploitBench) | 100% — تصنيف "Critical" | — | **Astra** | منشور (الهجومي مُقيَّد بـ Daybreak؛ الدفاعي مفتوح) |
| مستندات منسَّقة بقالب (DOCX/PPTX) | مُدرَّب صراحةً | جيد | **Astra** | منشور |
| "متى يسأل ومتى يفترض" | مُدرَّب صراحةً | جيد | Astra | منشور |
| **مراجعة ثابتة للسكربتات** | ✅ رصد خطر حذف حقيقي (ISSUE-036) | فاته | **Astra** | **ملاحَظ** |
| **دقة دلالات Wazuh** | ✅ صحّح `ignore` (ISSUE-037) | أخطأ | **Astra** | **ملاحَظ** |
| **صرامة منهجية القياس** | ✅ فصل الطوابع الزمنية، سجل محاولات مستقل (T-10) | قبِل `alerts.json` وحده | **Astra** | **ملاحَظ** |
| **توحيد نماذج ذهنية متعارضة** | — | ✅ `docs/05` | **Claude** | ملاحَظ |
| **سرد أكاديمي + مراجع مُتحقَّقة** | — | ✅ ch2/ch3 (15 مرجعاً Crossref) | **Claude** | ملاحَظ |
| **تحليل مصادر مختلطة (PDF/DOCX/صوت/صور)** | — | ✅ 7 مصادر، 90 لقطة | **Claude** | ملاحَظ |
| **تصميم البروتوكولات والقرارات المعمارية** | — | ✅ COLLABORATION، ADRs | **Claude** | ملاحَظ |

## 3. القيود المعروفة (تُنقل المهمة للآخر)

| القيد | عند | البديل |
|-------|-----|--------|
| **لا وصول لمعمل حي** — لا أحد منّا يستطيع تشغيل الأجهزة الافتراضية | كلاهما | الفريق البشري يُنفّذ runbooks/TEST_PLAN ويرفع الأدلة؛ الوكلاء يُحلّلون النتائج |
| **صلاحية GitHub Actions** — التوكن بلا `workflows` | كلاهما (ISSUE-038) | المستخدم ينقل الملف يدوياً |
| **إنشاء PoC استغلال هجومي** | Astra (مُقيَّد Daybreak) + Claude (يرفض) | المشروع دفاعي؛ محاكاة الهجمات تقتصر على أدوات قياسية (curl/nmap/EICAR/عينات Wazuh الرسمية) — **لا حاجة أصلاً** |
| **إنتاج DOCX بقالب جامعي منسَّق بدقة** | Claude (جيد لا ممتاز) | **Astra** (T-25) |
| **مراجعة أمنية عميقة للسكربتات** | Claude (فاته ISSUE-036) | **Astra** (T-15) |
| **إحصاء النتائج وتحليلها** (T-11/T-23) | Claude (كافٍ) | **Astra** أقوى رياضياً — يملكها |
| **قراءة لقطات الشاشة/الصوت** | Astra (حسب أدوات جلسته) | Claude أثبت القدرة — يملك تحليل المصادر |

## 4. التوزيع النهائي المُعتمَد (يُلغي أي توزيع سابق مُخالف)

### Astra يملك — "على النظام"
| المهمة | لماذا Astra |
|--------|-------------|
| **T-15** مراجعة أمنية + إصلاح `remove-threat.sh`/`yara.sh`/`yara.bat`/`scripts/**` | أقوى في الأمن السيبراني والمراجعة الثابتة (رصد 036) |
| **T-11** أداة القياس (سجل محاولات + alerts.json) | أقوى رياضياً + صرامته المنهجية في T-10 |
| **T-12** UC-09 SQL Injection | تنفيذ تقني على النظام |
| **T-13** UC-11 SSH brute-force + `firewall-drop` | تنفيذ تقني + استجابة آلية (جوهر رؤية المستخدم "الوكلاء تدافع") |
| **T-14** UC-10 Telegram/Email | تنفيذ تقني (سكربت Integrator) |
| **T-22** الفصل 4 (التنفيذ) | مستندات هندسية منظَّمة من runbooks + لقطات |
| **T-23** الفصل 5 (النتائج) | إحصاء + جداول + رسوم |
| **T-25** بناء DOCX النهائي بقالب الجامعة | مُدرَّب على القوالب صراحةً |
| **T-31 (تنفيذ)** UC-12 أجهزة الشبكة — decoders + قواعد 100400+ | تنفيذ على النظام (التصميم من Claude) |
| مراجعة **تقنية** لكل ما يكتبه Claude في `wazuh/` | عين ثانية أدقّ في دلالات Wazuh |

### Claude يملك — "العقل والسرد"
| المهمة | لماذا Claude |
|--------|--------------|
| `docs/00–05`, `DECISIONS.md`, البروتوكولات، هذا الملف | استدلال معماري وتوحيد |
| **T-24** توحيد الفصل 1 مع الواقع | تحليل التعارضات |
| ✅ T-20 الفصل 2، ✅ T-21 الفصل 3 | سرد أكاديمي + مراجع |
| **T-30** Demo Script للجنة | سرد + استباق أسئلة اللجنة |
| **T-31 (تصميم)** UC-12 — دراسة الجدوى والمعمارية | ثم يُسلَّم لـ Astra للتنفيذ |
| **T-32** الملخص التنفيذي + المقدمة العامة + الخاتمة الأدبية للرسالة | سرد |
| **T-33** مراجعة **لغوية/منطقية** للفصلَين 4 و5 بعد كتابة Astra | عين ثانية للسرد |
| هندسة السياق: إبقاء `00_PROJECT_STATE`, `TASKBOARD`, `SESSIONS_LOG` صادقة ومحدَّثة | ملكية التوثيق الحاكم |

### مشترك (بالحجز)
`wazuh/**` — من يحجز المهمة يملك الملف؛ الآخر يراجع بعد الدمج ويُسجّل ISSUE إن وجد.

## 5. قاعدة "لا تشينكس، لا حذف، لا تجاوز" (Zero-Skip Rule)

بتوجيه المستخدم، **يُمنع**:
1. تعليم مهمة ✅ وهي ناقصة — الحالة الصادقة: `◐ IN-PROGRESS` + "المتبقي بالضبط".
2. حذف أو تقليص نطاق متطلب (FR/NFR في ch3 §3.3) دون ADR في `DECISIONS.md` وموافقة المستخدم.
3. تجاوز فحص `validate_all.sh` أو مراجعة الآخر.
4. الادعاء بأن شيئاً "يعمل" بلا دليل مؤرَّخ (ISSUE-032).
5. إغلاق ISSUE بلا ذكر الـ commit أو الدليل.

## 6. هندسة السياق (Context Engineering) — إلزامي لكل جلسة

الهدف: **أي وكيل جديد يقرأ المستودع فقط ويعرف بدقة أين نحن وماذا يفعل** — بلا هلوسة، بلا افتراض.

| الملف | من يُحدّثه | متى | ما يجب أن يحتويه |
|-------|-----------|-----|------------------|
| `docs/00_PROJECT_STATE.md` | من أنهى مهمة | فور الدمج | آخر commit على main، % الإنجاز لكل مرحلة، **المهمة التالية بالضبط لكل وكيل** |
| `docs/TASKBOARD.md` | صاحب المهمة | عند الحجز، وعند الإنهاء، وكل ساعة عمل | الحالة + المتبقي بالضبط + commit |
| `docs/SESSIONS_LOG.md` | كل وكيل | نهاية كل جلسة (append-only) | ما أُنجز، commits، رسائل `@OTHER` |
| `docs/04_ISSUES_LOG.md` | من اكتشف/أغلق | فوراً | ID فريد، الموضع، الإصلاح، الحالة، الـ commit |
| `CHANGELOG.md` | من دمج PR | مع كل PR | نسخة semver + Added/Changed/Fixed |
| `docs/DECISIONS.md` | من اتخذ قراراً معمارياً | قبل التنفيذ | ADR: السياق، القرار، البدائل، العواقب، الحالة |

**قاعدة الـ 60 دقيقة:** كل 60 دقيقة عمل → commit + تحديث TASKBOARD (حتى لو لم تنتهِ المهمة) — لتقليل الضياع عند انقطاع الجلسة.

**قاعدة السطر الأول:** أي ملف جديد يبدأ بـ header: الغرض، المالك، الحالة، آخر تحديث، المصادر.

---

## 7. تدقيق دوري (كل 5 PRs)
يقوم Claude بمراجعة اتساق `00_PROJECT_STATE` ↔ `TASKBOARD` ↔ `SESSIONS_LOG` ↔ `CHANGELOG` ويُسجّل أي انحراف كـ ISSUE. آخر تدقيق: 2026-09-09 (PR #10) — انظر `SESSIONS_LOG`.
