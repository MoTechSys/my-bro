# سجل تدقيق الاستلام وخريطة الأدلة — T-16

> **خاتمة هذه الدفعة — 2026-09-23:** انتهت مراجعةcollector ccdc4a2c وحُكمت بنودها التسعة فيtests/README؛ لا تعاد. أضيف اتساق ساعةmanager/observer في6853581 واختباراه في414384d؛ **48 اختبارcollector و800 كليًا ناجحة على414384d**. ذكرrunning أو46/798 أدناه لقطة سابقة. محولات الدايمونات/Windows/controller وt4/t5 وISSUE-068 وبقية التدقيق ما زالت أعمالًا محلية؛ لا نشر أو قبول أصلي. رأس التسليم وCI النهائي فيPR28.

> **أحدث جولة: 2026-09-23 [AI] — §9 أدناه.** تقييم اكتمال قائم على الأدلة، وإعادة تحقق SVG واختبارات البرمجيات وبحث رسمي. **المشروع غير مكتمل 100%**؛ لا نسبة تقديرية ولا شهادة امتثال. §1–8 سجلات تاريخية، وليست قبولاً للرأس الحالي.

> الغرض: فصل ما فُحص مباشرة عما لم يُفحص؛ المالك ASTRA؛ آخر تحديث 2026-09-09.
> الحالة: IN-PROGRESS — تدقيق المستودع لا اعتماد المعمل. المصادر: GitHub #14/#17، وثائق المشروع، أوامر البيئة والاختبارات المحلية.

## 1. ما أُنجز فعلاً

- روجع diff التسليم #17 كاملاً (ستة ملفات) وقرئت موافقة CLAUDE وحدودها، ثم دُمج #17 (`6529c1b`).
- أعيد تأسيس #14 على main، مع حفظ صفّي سجل الجلسات وملكية ASTRA، ثم أُعيدت 19 حالة أمنية والفاحص الكامل بنجاح، ودُمج #14 (`b47f526`).
- حذف claude-tmp فقط بعد إثبات أن رأسه `1eb4768` سلف main. الحجز اللاحق T-16/T-11 عبر #18.
- تصحيح R1 (غلاف Linux باسم remove-threat.exe + helper، لا Windows exe على Linux)، وR3 (ctime قد يفشل مغلقاً)، والإبقاء على R2/ISSUE-039 (سباق تبديل الاسم).
- تصحيح واجهات الديمو وUC-03..08، الخلط بين 100200/100201، دورة العمليات 30 ثانية، وأرقام زمن لم تُقَس. التحقق الحي لا يزال مطلوباً.
- لم يتغير `docs/sources/**` ولا نطاق المشروع الحاكم ولا Rule IDs.

## 2. تغطية القراءة المباشرة

| المادة | التغطية الفعلية | ما بقي |
|---|---|---|
| README/START_HERE/protocol/allocation/state/taskboard/sessions/roadmap/decisions/issues | قراءة نصية مباشرة ومقارنة تسليم #17 | تدقيق اتساق متكرر مع كل PR |
| docs/05، docs/01، docs/02 | قراءة مباشرة؛ docs/01 تحليل سابق وليس إعادة قراءة الأصول | تحقّق فرضيات OVA والإصدارات على الأجهزة؛ لا prompt كدليل |
| DEMO_SCRIPT + UC-03..08 + wazuh/README | قراءة وتدقيق واجهات مقابل #14 | dry-run مرتان مؤرختان؛ ربط كل تنبيه بمحاولته |
| TEST_PLAN وSECURITY_REVIEW | قراءة عقد القياس وحدود الأمن؛ إعادة اختبار الأمن | Windows/Wazuh/YARA، المنصّبات والـrollback أصلياً |
| scripts/** وAR | جرد ومراجعة T-15 السابقة مع 19 اختباراً أعيدت بعد الدمج | زيادة الاختبارات الأصلية؛ لا نستبدلها بـsyntax |
| wazuh/auditd/wazuh.rules | قراءة مباشرة؛ AUID=1000 وGID=994 تاريخي، installer يستبدل GID | إثبات UID/GID والسياسة المحملة فعلياً |
| الفصول الحالية | ch3 كامل 449 سطراً قرئ في T-17؛ مصفوفة 13 FR/8 NFR في الدراسة | تصحيحات ISSUE-047، تدقيق ch1/ch2 والمراجع وthesis/README تفصيلياً |
| S1..S5 الأصلية | نص S1 كل 8 صفحات وS2 كل 55 صفحة؛ S3/S4/S5 نص الجسم والجداول من XML مباشرة؛ مقارنة فقرات النطاق | تدقيق كامل الصور/التنسيق ليس منجزاً؛ النص المستخرج لا يكفي لكل ادعاء بصري |
| الصور والصوتيات | S6/S7 تحليل محتوى ثم Scribe v2 من MP3 محول للأصل، مدة مثبتة بـffprobe؛ S2 صفحات 4/19/30/39/49 فحص بصري آلي | تأكيد العبارات الغامضة؛ توقيت التحليل الأول مرفوض؛ provenance/التاريخ/إصدار Kali في التحليل البصري غير موثوق بالكامل؛ بقية الصور باقية |

لا تعني عبارة «الفاحص ناجح» صحة كل ادعاء أكاديمي أو تجريبي. لا تُغلق ISSUE-046 حتى توثيق تغطية البقية.

## 3. البيئة التي فُحصت

Linux، Python 3.13.14، Git 2.47.3، ذاكرة نحو 7.8 GiB ومساحة حرة نحو 20 GiB وقت الفحص. متاح: gh، jq، xmllint، pytest، pandoc، libreoffice، pdftotext، node، gsk. غير متاح في PATH: shellcheck، pwsh، yara، docker؛ ولا wazuh-analysisd في المسار الأصلي.

هذه ليست أجهزة SOC. لم يبدأ خادم HTTP، ولم تُنفذ طلبات هجومية أو تنزيل عينات أو تثبيت خدمات. مهارات الاستضافة وتصميم المواقع ليست مطابقة للمهمة؛ لا فائدة من تشغيلها لمجرد توفرها. يمكن استخدام أدوات المستندات لاحقاً في T-25، بعد قالب الجامعة ومراجعة المخرجات بصرياً.

## 4. ترتيب استكمال التدقيق (لا يُتجاوز)

1. T-17 يسبق متابعة التنفيذ بتوجيه المستخدم؛ عرض قرارات الدراسة ثم استكمال T-11: عقد JSONL صريح، manifests، إخراج المحاولات المستبعدة وأسبابها، اختبارات السجلات التالفة والربط والوقت.
2. مراجعة FR/NFR في الفصل 3 وربط كل معرّف بملف تنفيذ واختبار ودليل أو blocker؛ مراجعة الفصلين 1 و2 والمراجع، دون إعادة إعلان تحقق DOI السابق كتحقق جديد.
3. قراءة الأصول بترتيب S4 ثم S5/S2 ثم S1/S3؛ اختيار اللقطات من المعمل فقط للنتائج؛ إبقاء الأصول بلا تعديل. النص العربي المستخرج قد يكون مكسوراً ويتطلب صور الصفحات.
4. تدقيق UC-01/02 وsuricata config وواجهات التثبيت/firewall القديمة أيضاً؛ لا اعتماد أمر يفعّل جداراً نارياً دون تأمين قناة الإدارة والاستعادة.
5. دورة P1 على الأجهزة مع snapshots ونسخ إعدادات منقحة؛ ثم PILOT وMEASURED وBASELINE حسب TEST_PLAN.
6. عند كل تعارض: ISSUE فريد؛ عند تغيير FR/NFR أو النطاق: ADR وموافقة المستخدم، لا إسقاط صامت.

## 5. بروتوكول الوكيل التالي

ابدأ بـSTATE وTASKBOARD ثم هذا السجل وTEST_PLAN. نفذ fetch صريحاً لـmain وفرع التطوير، وافحص status وPRs والعمل غير المدموج قبل أي مزامنة. احجز المهمة أولاً. قبل إنهاء الجلسة: اختبارات، commit، PR، حالة/أسباب/التالي بدقة وروابط. سجل التاريخ التنفيذي بالـUTC الحقيقي من البيئة؛ لا تستنتج ترتيب الوقت من صفوف الجلسات القديمة المتداخلة.

المطلوب من الفريق: Q5 الموعد، Q6 القالب، جرد VM فعلي، ضبط Windows/ACL، manifests معتمدة، وأدلة القياس المنقحة. لا ترفع أسراراً إلى المستودع العام.

## 6. نقطة التوقف بعد نواة T-11

#19 دمج خطة الاستلام. #20 يضيف نواة كشف واختباراتها وعقد tests/README؛ 53 اختباراً محلياً ناجحاً. المصادر الأصلية والفصول لم تُراجع بالكامل أثناء هذه المرحلة. ابدأ باستكمال T-11 كما في tests/README «المتبقي»؛ لا تخلط اكتمال PR النواة باكتمال المهمة. الفرعان الوحيدان على origin هما main وgenspark_ai_developer بعد إثبات ancestry وحذف agent/astra وagent/claude أيضاً.

## 7. نقطة التوقف الأحدث — T-17، تتقدم على §6 التاريخي

PR #21 حجز الدراسة؛ #22 يوثق التحليل/المخطط والبحث الرسمي وتصحيحات القدرات. الدراسة مستندة إلى الأصول لا ملخص docs/01 وحده؛ لا تدعي تفريغاً بشرياً أو فحص كل لقطة. تحليل الصور استنتج ملكية لقطات Ubuntu22.04/2024 ووصف سبتمبر 2026 بالمستقبل؛ رُفضت الاستنتاجات. التفريغ الزمني الثاني داخل مدد الملفات لكنه غير مصدق كلمة بكلمة. راجع الدراسة §2/4 وISSUE-049.

التوقف: تسليم الدراسة للمستخدم وطلب D1–D7، ثم استكمال النواة غير المتنازع عليها؛ لا بدء توسعة هاتف/AI/واجهة جديدة من المخطط وحده. لم تتغير الأصول أو كود التشغيل. T-16/T-11/T-15 غير مكتملة، ونشر AR ما زال محكوماً ببوابات SECURITY_REVIEW.


## 8. تدقيق PR28 والبحث وخطة الاستكمال — 2026-09-18 [AI]

### 8.1 الأساس والتحقق

- سُحب الفرعان وفُحص العمل غير المدموج: main=e51dd7d وPR28=4f62a15 عند البداية. استُخدم فرع التطوير الموجود دون إسقاط تاريخ المتعاونين. تحقق حساب MoTechSys وصلاحية push عبر API دون طباعة أسرار.
- خط الأساس: **301 اختبار ناجح** مع validator وdiff check.
- أُعيد إنتاج تضخيم C4: ثلاثون case بأسماء batch/cluster مختلفة ونفس input_sha256 وA1 تعطي Wilson lower≈0.88649. إصلاحa2ba875 يرفض تكرار(hash,ref)، ويحجب الفاصل عند اشتراك export ولو اختلفت refs. البصمات ما زالت تصريحات غير مصادق عليها.
- أُعيد إنتاج تسرب HTTPException: BadStatusLine يحمل علامة نصية اصطناعية ويهرب من حد الخطأ العام. إصلاحd13ff7f يحوله إلى MODEL_REQUEST_FAILED مع خطأ CLI عام وstdout فارغ.
- **307 اختبارات ناجحة** بعد الإصلاح:145 قياس/أمان سابقة +67 lifecycle +48 analyst +19 knowledge +28 C4. أضيفت6 methods معsubtests. اختبارات جديدة شُغلت في الذاكرة ضد4f62a15 وكشفت السلوك القديم: أربعةC4 (2 failures/2 errors) وHTTP test بستةsubtest errors. تمر جميعها على الإصلاح الحالي؛ هذه ليست نتائج نموذج أو SOC.
- ROADMAP في1a0eba6 تصحح النطاق وCI والعينة/baseline وتحدد G0–G8 والمصادر وحدود القراءة. أرقام الإصلاحات نقاط حفظ قبل جمع تغييرات الجلسة؛ الرأس النهائي وCI في PR28.

### 8.2 تغطية الجولة وحدودها

| المادة | ما فُحص الآن | المتبقي |
|---|---|---|
| analyst/evaluate/knowledge | قراءة المصادر كاملة وعقودها، اختبارات analyst/C4 كاملة وإعادة حزمة knowledge | نموذج حي، تدقيق corpus كامل، جرد حقيقي، importer/deadline وتقييم دلالي |
| agent_health/agent_lifecycle | قراءة المصدرين كاملة، مراجعة guard/watch/service وإعادة67 اختباراً | لم تُقرأ كل525 سطر اختبار سطراً بسطر الآن؛ لا Docker/proc/native جديد |
| وثائق الدخول والحالة والتعاون والخطط وdocs01/02/05 | قراءة مباشرة ومقارنة النطاق والتواريخ والعقد؛ CHANGELOG الحالي ومداخل ذات صلة | تناقضات مرجعية وأكاديمية في076 و045/047/056/063؛ لا إعادة تدقيق كامل المراجع |
| INTENT_STUDY | §§1–4 مباشرة، وجرد العناوين ومصفوفةFR/NFR والقرارات | باقي النص والمخططات وإعادة مطابقة الأصول |
| mttd/trial_runner وAR القديمة | عقود tests/README وSECURITY_REVIEW، جرد وإعادة اختبارات؛ ليس تدقيقاً جديداً لكل1594 سطر مصدر | مراجعة مصادر T-11/T-15 وnative clocks/AR/Windows/PILOT |
| CI/validator | قراءة كاملة وإعادة محلية؛ بصماتactions وصلاحيات القراءة محفوظة | سجلات الرأس النهائي فيPR28، لا استعارة نجاح سابق |
| الأصول والفصول والصور والصوت | لم تُعد قراءتها أو معالجة صورها أو التحقق منDOIs في هذه الجولة؛ لم تتغير | بقيةT-16/ISSUE-046 والفحص البصري والأكاديمي |

### 8.3 المراجعة الآلية الثانية والتحكيم

[مهمة المراجعة](https://www.genspark.ai/agents?id=34016d8e-2862-5e9c-87f0-f7574bf3e743) تلقت النص الكامل لخمسة ملفات تنفيذ وtest_ai_evaluate عند4f62a15 للقراءة فقط. انتهت succeeded وقرئ التقرير. الرابط خاص بالمالك ومن شاركه؛ **ليس التقرير موافقة على كامل PR أو الإصلاحات اللاحقة، ولا مراجعة بشرية**. الادعاءات التالية أُعيد تقييمها محلياً:

| الملاحظة | القرار والدليل |
|---|---|
| BUG-2 تكرار input_sha256 | مؤكد قبل ورود التقرير وأصلحa2ba875؛ نرفض نسخ Aref نفسه ولا نكتفي بحجب الفاصل. لا نعتمد تعليل أن temperature0 يضمن حتمية النموذج |
| BUG-1 XML declaration/BOM | التعريف في بداية ملف كامل يرفض فعلاً: تحسين توافق منخفض077. **BOM وحده قُبل والقواعد الافتراضية13 قُبلت**؛ ادعاء BOM وتعطل الافتراضي وتصنيف High غير مقبول |
| BUG-3 analyze({}) | KeyError مؤكدة في استدعاء Python داخلي ناقص؛ CLI يبني context. تحسين منخفض078. اقتراح .get('alerts',[]) مرفوض لأنه يحوّل التلف إلى نجاح فارغ |
| BUG-4 حجم UTF8 مقابل ASCII | حدان مستقلان معلنان في UC-14 §2؛ التوسع قد يسبب رفضاً محافظاً. أُضيف توضيح، دون إزالة حد الذاكرة |
| BUG-5 الاعتماد على.git | شرط بيئة الاختبار الحالية؛ قابلية اختبار أرشيف بلا.git تحتاج معالجة ضمنG8. لا يُنقل TMP خارج نطاق كتابة الجلسة |
| N1 socket timeout | قيد معروف070؛ G5a يطلب deadline شاملاً وتنظيف الموارد؛ لم يُصلح في هذه الجولة |
| N2 نافذةprogress>180 وN3/N4/N5 | حراسة محافظة وافتراضات صيغةstate/process-list تحتاج توافقاً أصلياً؛ لا تخفيف للضوابط بناءً على فرضية. بوابات064/068 باقية |
| N6 إسقاط curated notes عند تغيرhash | سلوك معلن بعلم صريح مع بقاءmetadata؛ لا اعتماد شروح XML غير مراجَع |

ذكر المراجع23 اختباراً، بينما حزمة4f62a15 بها24C4؛ لا نكرر رقمه. وصُححت تعليقة PR خاصة بنا من32 إلى28 بعد الرجوع إلى خرج unittest. العدد الكلي307 من تشغيل فعلي، لا من جمع غير متحقق.

### 8.4 الاستئناف

T-16/T-70 IN-PROGRESS. التالي محلياً ROADMAP §3: عقد artifact/importer، journal، deadline، قبل نموذج حقيقي. اتساق batch/status/output يحتاج تحديد معنى output_sha256 في عقد المستورد؛ لا نفرض رفضاً جديداً على evaluator بافتراض غير موثق. مسار السحابة منفصل: حماية config، حفظ الهوية، استعادة وتعافٍ وcanary لدى مشغّل مخوّل، ثم native/PILOT. لا اتصال أو نشر سحابي أو هجمات أو تنزيل نموذج في هذه الجولة.


## 9. تدقيق الاكتمال والمتجهات والمعايير — 2026-09-23

### 9.1 الحكم وحدود التغطية

**لا، المشروع ليس مكتملًا 100%.** توجد مكونات منفذة ومختبرة محليًا، وأعمال برمجية محلية لم تنفذ، وبوابات أجهزة وبيانات ومراجعة بشرية. لا يصح حساب نسبة من عدد الملفات أو الاختبارات؛ لم يُعتمد مقام موزون لمتطلبات المشروع ولا دليل قبول لكل متطلب. لا يُوصف المتبقي كله بأنه عائق بيئة.

بدأت هذه المتابعة من `6e81c0255a1b3175a630aac6992059460196979e`، على `genspark_ai_developer` في `/home/user/webapp/my-bro`؛ المستودع الأب مشروع مختلف لم يُعدل. فُحص main والفرع البعيد وPR28؛ حُفظ التاريخ المشترك. اجتاز هذا الرأس **668 اختبارًا في 28.227 ثانية** مع `ALL CHECKS PASSED`. هذا دليل اختبار محلي لا قبول SOC. CI لكل رأس لاحق يوثق في PR28، ولا يُنسب نجاح الرأس السابق إليه.

التغطية المباشرة لهذه الجولة: كود مقام AR واختباراته، منطق health/lifecycle، عقد القياس، ملفات الحالة والخطة والنطاق والتدقيق، الفصلان2/5 ودليل الكاتب، وبايتات ومصادر الرسومات الخمس عشرة. تحقق hashes للمصادر الرسمية المؤرشفة. لم تُراجع جميع أسطر المستودع أو كل الصور والمراجع الأصلية، ولم يُختبر Wazuh أو Windows أو YARA أصليًا أو يتصل بنموذج حقيقي. لا اعتماد لمعيار NIST أو شهادة أمنية.

### 9.2 مصفوفة الاكتمال الفعلية

| المجال | الموجود والدليل المحلي | ما يمنع إعلان الاكتمال | نوع المتبقي |
|---|---|---|---|
| الرسوم/تسليم الكاتب | 15 SVG و15 Mermaid وcatalog، 36 اختبار writer، خمس مسودات وZIP ببصمات | مراجعة علمية/بصرية بشرية؛ تدقيق جميع المراجع والفصول | تحرير محلي + اعتماد بشري |
| سجل المحاولات والمصدر والرؤية | trial_runner، source_observer، visibility_observer، recovery مع اختبارات الربط والانقطاع | قبول المصدر والساعة وظهور Indexer على الجهاز الفعلي | قبول أصلي |
| نتائج أدلة AR | ar_policies اختياري، ar_outcome/ar_summary، 39 اختبارًا (27+12) بعد التحكيم | منتجا t4/t5 والربط السببي الأصلي؛ المراجعة الآلية محكّمة | مراجعة + برمجة محلية + قبول |
| UC-01 | health/progress ومحللconnection_measure مستقل؛72اختبارًا اصطناعيًا | جامع وربط أصلي لإقرارات الخدمة والحالة والحدث ثم قبول خمس دورات | برمجة محلية ثم تجربة |
| سلامة AR | حمايات Linux، مسارات Windows واختبارات اصطناعية | ACL وname-swap وWazuh/YARA وبناء Windows واختبارات رفض أصلية | تحقق وتصحيحات حسب الأجهزة |
| دورة الحياة | health/lifecycle مرشحان، وسجل إصلاح init/volumes تاريخي | ISSUE-068 لحماية config دون تخفيف guard؛ reboot/daemon/rollback | تصميم وبرمجة محلية + تشغيل مخول |
| AI الاستشاري | analyst/knowledge/runner/evaluate/report، نقل HTTP اصطناعي وحفظ الأدلة | نموذج ورخصة/موارد وهوية runtime وجرد معتمد و30 label بشرية وC4 أصلي | بيانات/موارد/بشر وتجربة |
| SSH/SQLi/Telegram | أدوات محدودة opt-in واختبارات | تنبيهات ورسالة مستلمة وحظر/فك حظر فعلي وسلامة قناة الإدارة | قبول أصلي |
| الشبكة/الهاتف | تصميم ونطاق مشروط؛ لا ادعاء decoder إنتاجي مكتمل | عينة syslog وطوبولوجيا وقرار نطاق؛ ثم التنفيذ المختص | مدخلات/قرار ثم برمجة |
| التقييم التجريبي | خطة PILOT5 ثم planned_n≥30 وbaseline≥12h ومقامات | لا بيانات أصلية مكتملة أو اعتماد إحصائي/تقني بشري | تجارب وبشر بعد جاهزية الأدوات |
| الرسالة والعرض | فصول1–5 ودليل وأشكال وديمو توثيقي | تدقيق المراجع/الاقتباسات، front matter والملاحق، dry-run مرتان والقالب | تحرير محلي + بيانات/بشر |

هذه مصفوفة مكونات وبوابات، وليست تتبعًا مكتملًا لكل FR/NFR. مصفوفة المتطلبات التفصيلية في دراسة النية تحتاج ربط كل متطلب بدليل قبول أصلي قبل الإغلاق النهائي.

### 9.3 الرسومات Vector فعلًا

فُحص XML لكل SVG: لا `image` أو `script` أو `foreignObject`، ولا href خارجي أو `data:`؛ عناصر هندسية ونصوص متجهية، وليست PNG مغطاة بامتدادSVG. أعاد `build_writer_package.py --check` تطابق31 ملفًا مولدًا. الصور النقطية السابقة استُخدمت للمراجعة فقط وليست بديل التسليم. JSON هو المصدر؛ Mermaid مشتق قابل للتحرير، **لم يُختبر بمحرك Mermaid**. لا ضمان لتخطيط النص في كل الخطوط/العارضات؛ تفاصيل العينة البصرية والتحكيم السابق في WRITER_HANDOFF §10.

نماذج البيانات تمثل JSON/JSONL/OpenSearch الفعلية، لا مخطط SQL مخترع. fig15 بطاقات تعريف لا يفرض t3 قبل t4. لا سلطة تنفيذ للنموذج: `execution_authority=none`.

### 9.4 البحث وتطبيق المعايير

المصادر الخمسة وبصمات الأصل والأرشيف وتاريخ الاسترجاع في `research/inbox/2026-09-23_standards_sources.json`. قرئت صفحات النشر والملخصات، وصفحة دورة حياة الوكيل، وصفوف مهام محددة من PDF الخاص بـSSDF1.1. لم يُقرأ PDF الخاص بـSP800-61r3 كاملًا. التفصيل في `research/analyzed/2026-09-22_completion_research.md` §6.

| المرجع/الممارسة | التطبيق في المشروع | الدليل المتاح | النقص الذي يمنع ادعاء امتثال |
|---|---|---|---|
| NIST SP800-61r3، نهائي أبريل2025، حل محل Rev.2 | ربط الجاهزية والكشف والاستجابة والتعافي بإدارة المخاطر CSF2.0؛ الفصل بين فحص YARA والاحتواء | TEST_PLAN وSECURITY_REVIEW وبوابات التعافي | لا سياسة استجابة مؤسسية معتمدة أو تمرين شامل |
| SSDF1.1 PO.1 / PO.4 | متطلبات أمان ومعايير تحقق وبوابات قبول | docs/05 وDECISIONS وTEST_PLAN والتدقيق | تتبع كامل لكل متطلب واعتماد المخاطر المتبقية |
| SSDF1.1 PS.1 | حماية الكود/config والأدلة وفق أقل صلاحية | Git/PR وحدود الملفات والاختبارات | لم تُثبت branch protection/ACL الفعلية؛ ISSUE-068 مفتوح |
| SSDF1.1 PS.2 / PS.3 | بصمات إصدار وحزمة حصرية وأرشفة المصادر | MANIFEST وZIP ومراجع research المؤرخة | hashes ليست توقيع هوية؛ سياسة احتفاظ/توزيع وأرشفة إصدار تشغيلي معتمدة |
| SSDF1.1 PW.7 / PW.8 | مراجعة الشيفرة واختبارات السلوك والأعطال | تقارير مراجعات آلية محكّمة واختبارات محلية وCI | مراجعة مختصين وقبول الثنائيات/الأنظمة الأصلية |
| SSDF1.1 RV.1 / RV.3 | متابعة العيوب وتوثيق السبب الجذري والاختبار المضاد | ISSUES وتحكيم المراجعات وregressions | مالك دوري لتحديث الثغرات واعتمادات الإصدار والاستجابة لها |
| SSDF1.2، SP800-218r1 IPD، 17ديسمبر2025 | متابعة تغير الإرشادات، لا استبدال مرجع نهائي بمسودة | صفحة الإصدار المؤرشفة | الوثيقة المشار إليها مسودة أولية وليست معيارًا نهائيًا |
| Wazuh current: agent life cycle | active ليس دليل اتصال جديد؛ keepalive/freshness جزء من تصميمUC-01 | الحالات never connected/pending/active/disconnected موثقة | default15m للانقطاع في الصفحة ليس قياس إعداد المعمل؛ يلزم جرد فعلي |

الاسترشاد بمهام مختارة من SSDF لا يساوي مطابقة جميع الممارسات أو شهادة امتثال. لا نستخدم حد15دقيقة بدل نافذة300ثانية لاختبارUC-01، ولا ننتظر disconnected لتخمين وقت القطع: نحتاج دليل دورة جديدًا لا حالة cached.

### 9.5 ترتيب العمل المحلي التالي ومعيار الإغلاق

1. **تحكيم AR مكتمل:** مراجعة46bbb5ff للقطة5bd2feb انتهت؛ لا تعاد. عولجF3/F6، وحُفظتشخيصF2 وأضيفscope لـF5 وتشخيصF7؛ حُكمF1/F4/F8/F9 مع اختبارات وتعليل فيtests/README. الإصلاحfb4100a و83ebff9؛680 اختبارًا علىdd87465. هذه مراجعةآليةساكنة لا اعتمادبشري أوnative.
2. **M2-B/t4:** تثبيت هوية حدث AR وربط trigger/run/trial ووقت دخول الاستجابة الفعلي بأدلة خاصة bounded؛ اختبارات إعادة استخدام الهوية والتكرار والمهل والانقطاع والملف/الرابط غير الآمن. لا تغير سلطة AR أو تسمح بمدخل مسار غير موثوق.
3. **M2-B/t5:** مراقب مستقل وربط بالهدف والعملية/الحدث الفريد؛ فصل عدم وجود الملف عن سبب اختفائه. حذف بشري/Defender/إعادة إنشاء هدف أو PID معاد الاستخدام لا يُحسب نجاحًا؛ عدم الحسم يبقى غير محسوم. افصل اكتمال فحصYARA عن نتيجة مطابقة108001.
4. **M3/UC-01:** عقد دورات وcollector/evaluator read-only مع prior state وهوية وكيل وحدث جديد وقياس freshness، مهلة300ث وpoll5ث وفق الخطة. حالات stale active/re-enrollment/missing/timeout/clock jump ضمن الاختبارات. الصحة التشغيلية لا تحل محل ذلك.
5. **ISSUE-068:** تصميم حماية متوافق مع ملكية Wazuh الأصلية، وفصل config الموثوق عن الحالة القابلة للكتابة؛ اختبارات رفض path/ACL قبل النشر. لا recursive chown ولا تخفيف protected().
6. **الأكاديمي:** تدقيق الفصلين1/4 وبقية2 والمراجع الأصلية والصور، ربط claim بمصدر/موضع، واستكمال الملاحق/العرض. لا تعدل الأصول أو النطاق صامتًا؛ اعرض التعارض كـISSUE/ADR.

بعد نجاح كل مسار محلي: مراجعة مختصة، نشر مصرح به على بيئة مناسبة، إثبات الساعات والنسخ، ثم PILOT/MEASURED/BASELINE المستقلة حسب الحالة؛ C4 يحتاج البشر والنموذج والجرد. نجاح Linux لا يغلق Windows، ولا يلزم تعطيل كل تجربة Linux إلى حين توفر Windows.

### 9.6 انجراف التوثيق والقبول النهائي

رُصدت عبارات قديمة: AI خارج المنفذ في TEST_PLAN، خلط YARA بالاحتواء وRev.2 كمرجع راهن في ch2، وصف source/t3 ومقامAR كأنها جميعًا غير منفذة في ch5/دليل الكاتب. تصحح في هذه الجولة مع حفظ التاريخ. claims الدراسات المقارنة وPCI/التراخيص وأصالة الصور تحتاج تدقيقًا إضافيًا؛ لا يعد التحقق من DOI تحققًا من صحة تفسير الورقة.

قرار الإغلاق النهائي يتطلب: اكتمال الأعمال البرمجية أعلاه، متطلب→اختبار→دليل قبول، native security/recovery، بيانات تجريبية أصلية مع المقامات والمفقود، مراجعة إحصائية وبشرية، مصادقة النسخة الجامعية والنطاق، وبيان مخاطر متبقية يوافق عليه المالك. حتى ذلك الحين الحالة **IN-PROGRESS**، وليس «جاهز100%» أو «لم يبق إلا البيئة».


## 10. استكمال محلل UC-01 — 2026-09-23

الجولة فوقdf09bee فيالمستودع الصحيح؛ لم يتغير الأب أوالخادم أوإعداداتAR. المحلل الجديد connection_measure.py مستقل عنMTTD ويقرأ صادرات خاصة فقط. خطة5..100دورة وهوية/إعداد وساعات، سجلات مطبعة، تنبيهات خام. يحفظ مقام الدورات المخططة، ويُظهر المفقود والاستبعاد والنزاعات. raw matching ليس أصالة أوسببية.

التنفيذ c8e8f66 والاختبارات be7a45c؛ مراجعة6f1083f8 ساكنة انتهت ومحكّمة فيtests/README. عولجت استمراريةold/new في614f9d3، مع تقوية محلية للساعة وJSONL وتوقيعاتcanary وactive بعد الحدث. **752اختبارًا محليًا في28.308s على614f9d3**، و31ملف رسوم مشتقًا متطابقًا. CIللرأس النهائي مثبت منفصلًا فيPR28.

المصدر الرسميagent_control مؤرشف وبصماته فيresearch/inbox/2026-09-23_uc01_source.json؛ قراءة وصف الخيارات والحالات، لا تشغيل أوإثبات إصدار المعمل. لا تُسمى مراجعة آلية مراجعة بشرية، ولا تغطي الإصلاحات اللاحقة تلقائيًا.

**لم يكتملUC-01 أصليًا:** الجامع read-only وbyte binding ومخزن الأدلة ما زالت برمجة محلية، ثم يلزم جرد وساعات وخمس دورات أصلية لكل نظام مؤهل. لا تكرار للمحلل أوالمراجعة المنجزة. M2-B/t4/t5 وISSUE-068 وبقية تدقيق الفصول والبيانات والبشر باقية؛ المشروع ليس100%.


## 11. جامع UC-01 وتدقيق جزئي للفصول — 2026-09-23

- متابعةa5fca55 فيالمستودعالصحيح؛ لا تغيير للمشروعالأب أوالسحابة، وPR28 محفوظ بتقدمfast-forward.
-46اختبارًا جديدًا: native-like parsers، fixed argv، private stores، raw mutation، partial writes/interrupts، strict types، precision/source/clock/identity binding، وreal-store end-to-end إلىالمحلل بمقام5 ودورةواحدةموثقة اصطناعيًا. الفاحص الكامل **798اختبارًا في32.226s على4b85b6f**؛ ليس قبولSOC.
-5مصادرversioned مؤرشفة معmetadata وhashes. قُرئغلافagent_control ووحدةWazuh وحقولsystemd المختارة؛ helperfiles مؤرشفة لايعني العثورعلىتعريفprint_agent_status فيها. لم يشغلnativequery.
-حدStockWazuh مؤكد منالوحدة: forking/RemainAfterExit بلاPIDFile. دعمactive/exited ببرهاندايمونات يحتاج برمجة، وكذلكWindows/non-systemd/controller capture. لا إغلاقT-11 منوجودالجامع.
-مراجعةآليةccdc4a2c للقطةc7acf13 أُرسلتبنصوصكاملة، وكانتrunning عندالتحديث؛ لا إعادةإنشائها أوتسميةالمراجعةالذاتيةمستقلة.
-قرئ الفصل1 كاملًا وصُححجزئيًا معقراراتالمشروعوالجردالتاريخي؛ وقرئالفصل4 كاملًا وصُححذكرالأدواتوالرسوموالبوابات. لمتعدقراءةكلصورS4 أوتحقيقكلادعاءنظري. 15SVGو31مخرجًا متطابقة دونتغيير.
-متبقي محلي مستقل: t4/t5، ISSUE-068، المحولات المذكورة، التكامل وبقيةالفصول/المراجع/الجردالسحابيالمصور. لا نتائجأصلية أوlabelsمختلقة، execution_authority=none.


## 12. محول procfs ومراجعته — 2026-09-23

- نُفذبديلLinuxلايعتمدsystemd:مسحانلخمسةدايمونات،هويةPID/start_ticks/boot/namespaces وexe path/inode،مخازنخاصة وexport وbind إلىالمحللالقائم.
-38اختبارًااصطناعيًا جديدًا؛838كليًاعلىc395a61 في32.597ث.اختباراتملفاتprocمصطنعة،لا قراءةلعملياتSOC.لا اختبارطاقةأونشرأوإعادةتشغيل.
-مصدرانمثبتانLinuxv6.12 وصفحتاman مؤرشفةبالـUTCوبصمتيالأصلوالضغط؛قرئتدلالةbtime/starttime وإزاحتيtime namespace.ليس ذلكجردنواةالمعمل.
-المراجعة08768699 انتهت وحُكمتD1..D6؛المراجعلميشغّلاختباراتالمستودع.التقريروالحدودفيtests/README.رفضاقتراحretry وتوسيعR/S؛أُصلحوصفD وأضافت3اختباراتتحكيمضمن38.
-الوقتالمصدرlatest required daemon birth ليسجاهزية.المحللschemav1 باقٍ؛معرفةbackend تحتاجintent/exports المرتبطة،والسجلالمجردلايحمله.هذهالدلالةصريحةبدليل الكاتب.
-متبقي:قبولnativeLinux،والبرمجةWindows/controller وt4/t5/config والتكاملوبقيةالتدقيق.15SVGلمتتغير و31مخرجًامتطابقًا؛لانتيجةSOCأواستقلال/أصالةمدعاة.
